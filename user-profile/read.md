SET/GET → store and retrieve a complete value for a key.

HSET/HGET → store and retrieve fields inside a Redis Hash, similar to properties of an object. bascially STORE Object

hgetall -> like getting entire object
